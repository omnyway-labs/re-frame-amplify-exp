(ns re-frame-amplify-exp.core-test
  (:require [cljs.test :refer-macros [deftest testing is]]
            [re-frame-amplify-exp.core :as core]))

(deftest fake-test
  (testing "fake description"
    (is (= 1 2))))
